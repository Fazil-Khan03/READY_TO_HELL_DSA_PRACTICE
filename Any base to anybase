1. You are given a number n.
2. You are given a base b1. n is a number on base b.
3. You are given another base b2.
4. You are required to convert the number n of base b1 to a number in base b2.

A number n
A base b1
A base b2

A number of base b2 equal in value to n of base b1.

Constraints

0 <= n <= 512
2 <= b1 <= 10
2 <= b2 <= 10

Sample Input

111001
2
3

Sample Output

2010


solution:

// first convert the given base number to decimal ,then convert that decimal to target base simple solution
#include<iostream>
using namespace std;
int main()
{
    int n,b1,b2;
    cin>>n;
    cin>>b1;
    cin>>b2;
   // b1base to deci
   int dec = 0 , fact = 1;
   while(n>0)
   {
       int rem  = n % 10;
       dec +=rem*fact;
       fact = fact * b1;
       n = n /10;
   }
    
    // dec to tbase
    
    int tbase = 0;
    fact = 1;
    while(dec)
    {
        
        int rem = dec % b2;
        dec = dec / b2;
        tbase += rem * fact;
        fact = fact * 10;
    
    }
    
    cout<<tbase;
  
    return 0;
}
